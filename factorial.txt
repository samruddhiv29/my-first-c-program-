// factoria progarm 
#include <stdio.h>

int factorial (int n) 
{
if (n==0)
return 1;
else
return n*factorial(n-1);
}
int main()
{
    int num=5;
    int result;
    result=factorial(num);
    printf("factorial of %d is %d",num,result);

    return 0;
}